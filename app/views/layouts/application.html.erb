<!DOCTYPE html>
<html lang="en">
<head>

  <meta charset="UTF-8">
  <title>QMi</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <meta charset="UTF-8">
  <meta http-equiv="content-type" content="text/html; charset=utf-8" />
  <meta name="description" content="" />
  <meta name="keywords" content="" />
  <!--[if lte IE 8]><script src="js/html5shiv.js"></script><![endif]-->

  <script src="js/jquery.min.js"></script>
  <script src="js/skel.min.js"></script>
  <script src="js/skel-layers.min.js"></script>
  <script src="js/init.js"></script>
  <noscript>
    <link rel="stylesheet" href="css/font-awesome.css" />
    <link rel="stylesheet" href="css/skel.css" />
    <link rel="stylesheet" href="css/style.css.erb" />
    <link rel="stylesheet" href="css/style-xlarge.css" />
    <link rel="stylesheet" type="text/css" href="css/custom_bs.css" />
  </noscript>
</head>
<body id="landing">
  <header id="header">
    <h1>QMi</h1>
    <nav id="nav">
      <ul>
        <li><%=link_to 'Home', '/' %></li>
        <!-- user_signed_in? : check if user signed in -->
        <% if user_signed_in? %>
          <!-- current_user : gives currently logged in user's object-->
          <li><%= link_to 'Edit Profile', edit_user_registration_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
          <% if current_user.role? %>
            <li><%= link_to 'admin', '/admin' %></li>
          <% end %>
        <% else %>
        <!-- unnecessary  <li><%= link_to "Sign Up", new_user_registration_path %></li>-->
          <li><%= link_to "Already have an account?", new_user_session_path, class: "button special" %></li>
        <% end %>
      </ul>
    </nav>
  </header>

  <% if notice %>
    <div class="devise_locales_styled"><%= notice %></div>
  <% end %>
  <% if alert %>
    <div class="devise_locales_styled"><%= alert %></div>
  <% end %>

  <%= yield %>


</body>
</html>
